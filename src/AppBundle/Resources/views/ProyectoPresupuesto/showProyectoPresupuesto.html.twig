{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProyectoPresupuesto</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Nombre Proyecto presupuesto</th>
                <td>{{ entity.nombrePresupuesto }}</td>
            </tr>
            {% for presupuesto in entity.presupuestoIndividual %}
             <tr>
                <th>Código Prespuesto Asociado</th>
                <td>{{ presupuesto }}  <a class="btn btn-info btn-xs" href="{{ path('horaspresupuesto_show', { 'id': presupuesto.id }) }}">
                    mostrar
                </a>
                </td>
            </tr>
           {% endfor %}
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('proyectopresupuesto') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a class="btn btn-default" href="{{ path('proyectopresupuesto_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
