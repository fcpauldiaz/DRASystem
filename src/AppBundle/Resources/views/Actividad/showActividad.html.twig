{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Actividad</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombreactividad</th>
                <td>{{ entity.nombreActividad }}</td>
            </tr>
            <tr>
                <th>Abreviatura</th>
                <td>{{ entity.abreviatura }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('actividad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('actividad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
