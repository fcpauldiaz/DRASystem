{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Actividad list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombreactividad</th>
                <th>Abreviatura</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('actividad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreActividad }}</td>
                <td>{{ entity.abreviatura }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('actividad_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('actividad_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('actividad_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
