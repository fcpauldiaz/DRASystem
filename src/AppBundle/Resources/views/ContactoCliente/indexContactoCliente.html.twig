{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContactoCliente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombrecontacto</th>
                <th>Telefonocontacto</th>
                <th>Correocontacto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contactocliente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreContacto }}</td>
                <td>{{ entity.telefonoContacto }}</td>
                <td>{{ entity.correoContacto }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contactocliente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contactocliente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contactocliente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
