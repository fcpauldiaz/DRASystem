{% extends '::base.html.twig' %}

{% block body -%}
    <br>
    <h1>Descuento list</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombredescuento</th>
                <th>Cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('descuento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreDescuento }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-default" href="{{ path('descuento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-default" href="{{ path('descuento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a class="btn btn-default btn-block" href="{{ path('descuento_new') }}">
                Crear nuevo registro
            </a>
        </li>
    </ul>
    {% endblock %}
