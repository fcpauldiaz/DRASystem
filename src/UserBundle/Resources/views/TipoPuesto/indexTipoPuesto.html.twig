{% extends '::base.html.twig' %}

{% block body -%}
    <br>
    <h1>TipoPuesto list</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombrepuesto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tipopuesto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombrePuesto }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-default" href="{{ path('tipopuesto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-default" href="{{ path('tipopuesto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a class="btn btn-default btn-block" href="{{ path('tipopuesto_new_post') }}">
                Crear nuevo registro
            </a>
        </li>
    </ul>
    {% endblock %}
