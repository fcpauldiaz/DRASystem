{% extends '::base.html.twig' %}
    {% block body -%}
        <h1>Usuarios de {{app.user.nombre}} {{app.user.apellidos}}</h1>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Apellidos</th>
                    <th>Puesto</th>
                    <th>Acción</th>
                    
                </tr>
            </thead>
            <tbody>
                {% for usuario in usuarios %}
                    
                <tr>
                    <td>{{usuario.usr.nombre}} </td>
                    <td>{{usuario.usr.apellidos}} </td>
                    <td>
                    {% if usuario.usr.puestos is defined %}
                        {{usuario.usr.puestos.last}}
                    {% endif %}
                    </td>

                    <td>
                            <a href="{{path('revisar_horas', { 'usuario_id':usuario.usr.id})}}"
                            class="btn btn-info btn-sm">Revisar horas</a> 
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
      
        <br>
        <br>
        <br>
    {% endblock %}
